import './Home.css';

function Home() {
	return (
		<section className="page-card hero-card">
			<p className="eyebrow">Home</p>
			<h2>Build with theme context API</h2>
			<p>
				This practice app now includes a shared theme context, a navigation bar,
				and separate home, about, login, signup, and dashboard pages.
			</p>
		</section>
	);
}

export default Home;